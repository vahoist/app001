// import { Pipe, PipeTransform } from '@angular/core';

// @Pipe({
//     name: 'joinPipe'
// })

// export class joinPipe implements PipeTransform {
//     transform(val: number, arr: Array<any>, start?: number, end?: number): String {
//         let result: Array<any> = arr;
//         if (start !== undefined) {
//             if (end !== undefined) {
//                 result = arr.slice(start - 1, end);
//             } else {
//                 result = arr.slice(start - 1, arr.length)
//             }
//         }
//         return arr[0];
//     }
// }
